#include <iostream>

using namespace std;
int main()
{
    int n, m;
    cin >> n >> m;
    int num[n][m];
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
            cin >> num[i][j];
        }
    }
    int row_start = 0, col_start = 0, row_end = n - 1, col_end = m - 1;
    for (int i = 0; i <; i++)
    {
        /* code */
    }
    
    return 0;
}